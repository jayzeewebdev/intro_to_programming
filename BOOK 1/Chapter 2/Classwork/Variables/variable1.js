//Example1
// Create a variable called 'greeting' and assign it the value "Hello"
var greeting = "Hello";

// Create a variable called 'myName' and assign it the value " JACK"
var myName = " JACK";

// Concatenate (join) the two strings and return "Hello JACK"
console.log(greeting + myName);


//Example2
//finding the length
'Jack'.length;

//Example3
// Create a variable called 'myName' and assign it the value "Jack"
var myName = "Jack";

// Access the character at index 0 (the first character) of 'myName'
// Then print it to the console. This will output "J"
console.log(myName[0]);

//Examlpe9
// Create a variable called 'height' and assign it the value 65
var height = 65;

// Create another variable called 'heightRestriction' and assign it the value 60
var heightRestriction = 60;

// Compare if 'height' is greater than 'heightRestriction'
// Since 65 > 60, this will return true
console.log(height > heightRestriction); // Output: true


